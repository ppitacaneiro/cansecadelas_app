---
const { card } = Astro.props;
---
<div class="bg-gradient-to-b from-blue-900 to-blue-600 rounded-lg shadow-lg h-full">
    <div class="p-5">
        <h3 class="text-3xl font-bold text-cyan-500 font-serif">{ card.title }</h3>
        <p class="mt-3 text-white">{ card.text }</p>
    </div>
</div>